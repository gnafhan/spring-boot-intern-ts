# ==============================================
# Development Environment Variables
# ==============================================
# Use this for local development without Traefik

# Database
POSTGRES_DB=interndb
POSTGRES_USER=internuser
POSTGRES_PASSWORD=internpass
POSTGRES_PORT=5432

# Backend Port (exposed to host)
BACKEND_PORT=8712

# Frontend Port (exposed to host)
FRONTEND_PORT=6173

# Frontend API URL (points to local backend)
VITE_API_BASE_URL=http://localhost:8712/api

# Spring Boot
SPRING_PROFILES_ACTIVE=dev
SPRING_JPA_SHOW_SQL=true
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# Server Port (internal)
SERVER_PORT=8080
